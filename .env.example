# Optional SMTP settings. Without these, emails are logged but not sent.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-user@example.com
SMTP_PASSWORD=change-me
FROM_EMAIL=your-user@example.com
